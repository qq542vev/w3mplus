#!/usr/bin/env sh

## File: blocking
##
## Shows the malware protection page.
##
## Usage:
##
##   (start code)
##   QUERY_STRING='type={malware|phishing|spam}&source={url}&url={url}' bloking
##   (end)
##
## Options:
##
## Exit Status:
##
##   0 - Program terminated normally.
##   64<= and <=78 - Program terminated abnormally. See </usr/include/sysexits.h> for the returned value.
##
## Metadata:
##
##   author - qq542vev <https://purl.org/meta/me/>
##   version - 1.1.1
##   date - 2020-03-27
##   copyright - Copyright (C) 2019-2020 qq542vev. Some rights reserved.
##   license - CC-BY <https://creativecommons.org/licenses/by/4.0/>
##   package - w3mplus
##
## See Also:
##
##   * Project homepage - <https://github.com/qq542vev/w3mplus>
##   * Bag report - <https://github.com/qq542vev/w3mplus/issues>
##   * about:blocked - <about:blocked>

# 初期化
set -efu
umask '0022'
IFS=$(printf ' \t\n$'); IFS="${IFS%$}"
export 'IFS'

# initファイルの読み込み
: "${W3MPLUS_PATH:=${HOME}/.w3m/w3mplus}"
. "${W3MPLUS_PATH}/lib/w3mplus/init"

eval "$(printf '%s' "${QUERY_STRING%%url=*}" | parsequery --prefix 'query_')"
url=$(printf '%s' "${QUERY_STRING#*url=}" | htmlescape)
source=$(printf '%s' "${query_source}" | htmlescape)
title=$(printf '%s' "${query_title-${query_source}}" | htmlescape)
category=$(printf '%s' "${query_category-forbidden}" | htmlescape)

printHtml.sh --title 'Forbidden Site' - <<- EOF
	<h1>Forbidden Site</h1>

	<p>This Web page at <a href="${url}">${url}</a> has been blocked based on <a href="${source}">${title}</a></p>

	<p>To access this web page, change <a href="about:permissions">about:permissions</a> settings.</p>
EOF
