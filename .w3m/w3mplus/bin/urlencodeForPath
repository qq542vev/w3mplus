#!/usr/bin/env sh

awkScript=$(cat <<- 'EOF'
	BEGIN {
		RS = "/"
	}

	NR != 1 {
		printf(RS)
	}

	{
		gsub(/[']+/, "'\"&\"'", $0)
		system("printf '%s' '" $0 "' | urlencode")
	}
EOF
)

printf '%s' "${1}" |  awk "${awkScript}"
